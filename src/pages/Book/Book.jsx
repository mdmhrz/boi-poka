import React, { use } from 'react';

const Book = ({ singleBook }) => {

    // const data = use(bookPromise);
    // console.log(data);

    return (
        <div>
            <h1>Hello Single Book</h1>
        </div>
    );
};

export default Book;